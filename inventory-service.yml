server:
  port: 0   # puerto dinámico para registrarse en Eureka
spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/inventorydb
    username: martin
    password: martin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
