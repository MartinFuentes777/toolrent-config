server:
  port: 0
spring:
  application:
    name: client-service
  datasource:
    url: jdbc:mysql://localhost:3306/clientdb
    username: martin
    password: martin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
