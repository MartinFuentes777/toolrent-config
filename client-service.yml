server:
  port: 0
spring:
  application:
    name: client-service
datasource:
    url: jdbc:mysql://mysql:3306/clientdb
    username: client
    password: client
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true