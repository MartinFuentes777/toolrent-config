server:
  port: 0
spring:
  application:
    name: tariff-service
  datasource:
    url: jdbc:mysql://mysql:3306/tariffdb
    username: tariff
    password: tariff
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
