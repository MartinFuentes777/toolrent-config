server:
  port: 0
spring:
  application:
    name: tariff-service
  datasource:
    url: jdbc:mysql://localhost:3306/tariffdb
    username: martin
    password: martin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
