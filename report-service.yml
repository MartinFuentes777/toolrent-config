server:
  port: 0
spring:
  application:
    name: report-service
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/reportdb
    username: martin
    password: martin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
