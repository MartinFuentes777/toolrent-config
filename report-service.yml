server:
  port: 0
spring:
  application:
    name: report-service
  datasource:
    url: jdbc:mysql://mysql:3306/reportdb
    username: report
    password: report
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
