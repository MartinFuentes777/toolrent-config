server:
  port: 0
spring:
  application:
    name: loan-service
  datasource:
    url: jdbc:mysql://mysql:3306/loandb
    username: loan
    password: loan
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
