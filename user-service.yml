server:
  port: 0
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/userdb
    username: martin
    password: martin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
