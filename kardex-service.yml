server:
  port: 0

spring:
  application:
    name: kardex-service
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/kardexdb
    username: martin
    password: martin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
