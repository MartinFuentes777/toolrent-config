server:
  port: 0
spring:
  application:
    name: kardex-service
  datasource:
    url: jdbc:mysql://localhost:3306/kardexdb
    username: martin
    password: martin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
