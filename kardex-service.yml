server:
  port: 0

spring:
  application:
    name: kardex-service
  datasource:
    url: jdbc:mysql://mysql:3306/kardexdb
    username: kardex
    password: kardex
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
